import type Phaser from 'phaser';

/**
 * Textures for the start, pause and end screens (tiles, button faces,
 * backdrops). Owned by the screens workstream of the 2026-09-24 redesign;
 * called once from `BootScene`. Bake here; never use runtime filters.
 */
export function makeScreens(_scene: Phaser.Scene): void {}
